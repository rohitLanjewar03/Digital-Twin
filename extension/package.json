{
  "name": "email-assistant-history-connector",
  "version": "1.0.0",
  "description": "Chrome extension to connect browsing history with Email Assistant",
  "private": true,
  "scripts": {
    "generate-icons": "node generate_icons.js",
    "zip": "zip -r extension.zip . -x \"*.git*\" -x \"node_modules/*\" -x \"*.zip\" -x \"package-lock.json\""
  },
  "devDependencies": {
    "sharp": "^0.32.1"
  }
} 